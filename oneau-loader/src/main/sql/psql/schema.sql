"CREATE TABLE ONEAU.EPHEMERIS_HEADER(id SERIAL,NAME TEXT NOT NULL UNIQUE,FILENAME TEXT NOT NULL UNIQUE,KSIZE INTEGER NOT NULL,NUM_COEFF INTEGER NOT NULL,EPOCH_START NUMERIC NOT NULL,EPOCH_END NUMERIC NOT NULL,PRIMARY KEY (id));",
"CREATE TABLE ONEAU.CONSTANT(id SERIAL,HEADER_ID INTEGER NOT NULL,NAME TEXT NOT NULL,VALUE TEXT NOT NULL,PRIMARY KEY (id));",
"CREATE TABLE ONEAU.EPHEMERIS_DATA(id SERIAL,FILENAME TEXT NOT NULL UNIQUE,HEADER_ID INTEGER NOT NULL,PRIMARY KEY (id));",
"CREATE TABLE ONEAU.MEASURED(id SERIAL,NAME TEXT NOT NULL UNIQUE,DIMENSIONS INTEGER NOT NULL,CHEB_COEFFS INTEGER NOT NULL,COEFF_SETS INTEGER NOT NULL,PRIMARY KEY (id));",
"CREATE TABLE ONEAU.INTERVAL(id SERIAL,RANGE_FROM NUMERIC NOT NULL,RANGE_TO NUMERIC NOT NULL,PRIMARY KEY (id));",
"CREATE TABLE ONEAU.OBSERVATION(id SERIAL,FILE_ID INTEGER NOT NULL,MEASURED_ITEM_ID INTEGER NOT NULL,OBSERVATION_NUM INTEGER NOT NULL,INTERVAL_ID INTEGER,COEFFICIENT NUMERIC NOT NULL,PRIMARY KEY (id));",
"ALTER TABLE ONEAU.CONSTANT ADD FOREIGN KEY (HEADER_ID) REFERENCES ONEAU.EPHEMERIS_HEADER (id);",
"ALTER TABLE ONEAU.EPHEMERIS_DATA ADD FOREIGN KEY (HEADER_ID) REFERENCES ONEAU.EPHEMERIS_HEADER (id);",
"ALTER TABLE ONEAU.OBSERVATION ADD FOREIGN KEY (FILE_ID) REFERENCES ONEAU.EPHEMERIS_DATA (id);",
"ALTER TABLE ONEAU.OBSERVATION ADD FOREIGN KEY (MEASURED_ITEM_ID) REFERENCES ONEAU.MEASURED (id);",
"ALTER TABLE ONEAU.OBSERVATION ADD FOREIGN KEY (INTERVAL_ID) REFERENCES ONEAU.INTERVAL (id) ON DELETE CASCADE;",
